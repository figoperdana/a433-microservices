version: 2.1
jobs:
  lint-dockerfile:
    docker:
      - image: hadolint/hadolint:latest-debian
    steps:
      - checkout
      - run:
          name: Lint Dockerfile
          command: hadolint Dockerfile

  build-app-shipping-service:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build and push image
          command: |
            docker build -t ghcr.io/figoperdana/shipping-service:latest .
            echo "$GHCR_TOKEN" | docker login ghcr.io -u figoperdana --password-stdin
            docker push ghcr.io/figoperdana/shipping-service:latest

workflows:
  version: 2
  build:
    jobs:
      - lint-dockerfile
      - build-app-shipping-service